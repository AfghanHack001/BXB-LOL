**BXB BYPASS**

Termux Active Command:
```
apt update
apt upgrade
apt install git
pkg install python
pip install bs4
pip install colorma
pip install requests
```

BXB BYPASS COMMAND:
```
git clone
git add
git commit
```
